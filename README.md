# SquareGPS marketing page

Vite + React + TypeScript. Styles: CSS Modules + shared tokens in `src/styles/tokens.css`.

```bash
npm install
npm run dev
npm run build
```

Design source: Figma file `Squaregps`, node `24:130` (desktop frame `40:672`).
